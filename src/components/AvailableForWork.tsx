
export default function AvailableForWork() {
    return (
        <div className="inline-flex items-center gap-2 rounded-full bg-primary/10 px-4 py-2 text-xs font-semibold uppercase tracking-[0.2em] text-primary shadow-[0_8px_32px_rgba(31,38,135,0.07)]">
            <span className="h-2 w-2 rounded-full bg-primary animate-pulse-ring" />
            Available for work
        </div>
    )
}
